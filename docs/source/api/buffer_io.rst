BufferIO
========

Register in-memory bytes under string IDs so that
:py:meth:`Sound.fromFile` can load them as though they were on disk.
Useful for game-engine asset packs and Android APK / AAB bundles.

Upstream: :yse-cpp:`api/utils.html <YSE::BufferIO>`.

.. include:: _generated/buffer_i_o.rst
