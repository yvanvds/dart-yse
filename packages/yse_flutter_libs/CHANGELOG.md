# Changelog

## 0.1.0

- Initial release. Cross-compiles the YSE sound engine for Android via the
  NDK (arm64-v8a, x86_64 by default) so `package:yse` resolves
  `libyse.so` at runtime on Flutter Android.
